server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/taochanglian/cloud-config-repo.git
      uri: http://localhost:8888
  application:
    name: config-server
#  profiles:
#    active: git
